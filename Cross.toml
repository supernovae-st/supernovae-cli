# Cross-compilation configuration for SPN CLI
# Required for building on Linux ARM64 with keyring support

[target.aarch64-unknown-linux-gnu]
# Use custom Dockerfile with libdbus-1-dev pre-installed
dockerfile = ".cross/Dockerfile.aarch64-unknown-linux-gnu"
